name = "triagemovil"
compatibility_date = "2024-04-01"

# Required for Cloudflare Pages (BETA detection)
pages_build_output_dir = "dist"

# Optional: You can also specify the build command here
# build_command = "npm run build"
